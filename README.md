# Claude Code & Open Code 教程网站

这是一个介绍 Claude Code 和 Open Code 工具及 Skills 系统使用的教程网站。

## 功能特性

- 📚 **教程中心** - 从入门到进阶的完整教程体系
- 📖 **API 文档** - 详细的命令参考和配置说明
- 🎯 **Skills 目录** - 30+ AI Agent Skills 生态系统展示
- 💡 **交互示例** - 实际案例和代码演示
- 🎨 **明暗主题** - 支持明暗双模式切换
- 📱 **响应式设计** - 适配各种设备屏幕
- 🚀 **自动部署** - Cloudflare Pages CI/CD 集成

## 技术栈

- **Next.js 16** - React 框架（静态导出）
- **TypeScript** - 类型安全
- **Tailwind CSS 4** - 样式框架
- **shadcn/ui** - UI 组件库
- **next-themes** - 主题切换
- **Lucide React** - 图标库
- **Node.js 20** - 运行环境

## 项目结构

```
claude-open-code-docs/
├── app/                      # Next.js App Router
│   ├── layout.tsx            # 根布局（主题切换、导航栏）
│   ├── page.tsx              # 首页
│   ├── claude-code/          # Claude Code 专题页
│   ├── open-code/            # Open Code 专题页
│   ├── tutorials/            # 教程列表和详情
│   ├── skills/               # Skills 目录页面
│   ├── api/                  # API 文档
│   └── examples/             # 交互示例
├── components/               # React 组件
│   ├── ui/                   # shadcn/ui 组件
│   ├── layout/               # 布局组件
│   └── theme-provider.tsx    # 主题提供者
├── data/                     # 静态数据
│   ├── tutorials.ts          # 教程数据
│   ├── skills.ts             # Skills 数据
│   └── examples.ts           # 示例数据
├── lib/                      # 工具函数
├── public/                   # 静态资源
│   └── _redirects            # SPA 路由重定向规则
├── .nvmrc                    # Node.js 版本配置
├── next.config.ts            # Next.js 配置
├── DEPLOYMENT.md             # 详细部署文档
└── README.md                 # 项目说明
```

## 开始使用

### 安装依赖

```bash
npm install
```

### 运行开发服务器

```bash
npm run dev
```

打开 [http://localhost:3000](http://localhost:3000) 查看网站。

### 构建生产版本

```bash
npm run build
```

### 导出静态网站

```bash
npm run build
```

静态文件将生成在 `out/` 目录中，可以部署到任何静态网站托管服务。

## 🚀 部署到 Cloudflare Pages（自动更新）

本项目已配置 Cloudflare Pages 自动部署，每次推送代码到 GitHub 后会自动构建和部署。

预览：https://claude-open-code-skills-docs.pages.dev/

### 前置要求

1. **GitHub 仓库** - 将代码推送到 GitHub
2. **Cloudflare 账号** - 免费注册 [Cloudflare](https://dash.cloudflare.com/)
3. **Node.js 20+** - 项目使用 Node.js 20.11.1

### 步骤 1：准备 GitHub 仓库

```bash
# 初始化 Git 仓库（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库
git remote add origin https://github.com/your-username/your-repo.git

# 推送到 GitHub
git push -u origin main
```

### 步骤 2：配置项目文件

项目已包含必要的配置文件：

**`.nvmrc`** - 指定 Node.js 版本
```
20.11.1
```

**`next.config.ts`** - Next.js 静态导出配置
```typescript
const nextConfig: NextConfig = {
  output: 'export',      // 静态导出
  trailingSlash: true,   // URL 尾部斜杠
  images: {
    unoptimized: true,   // 图片优化（静态导出必需）
  },
};
```

**`public/_redirects`** - SPA 路由支持
```
/*  /index.html  200
```

### 步骤 3：连接 Cloudflare Pages

1. **登录 Cloudflare Dashboard**
   - 访问 [https://dash.cloudflare.com](https://dash.cloudflare.com)
   - 登录你的账号

2. **创建 Pages 项目**
   - 进入 **Workers & Pages** → **创建应用** → **Pages**
   - 点击 **连接到 Git**

3. **授权 GitHub**
   - 点击 **连接 GitHub** 按钮
   - 授权 Cloudflare 访问你的 GitHub 仓库
   - 从列表中选择你的仓库

4. **配置构建设置**
   Cloudflare 会自动读取 `.cloudflare-pages-build.json`，你也可以手动确认：

   | 设置项 | 值 |
   |--------|-----|
   | 项目名称 | `your-project-name` |
   | 生产分支 | `main` |
   | 构建命令 | `npm install && npm run build` |
   | 构建输出目录 | `out` |
   | Node.js 版本 | `20` |

5. **保存并部署**
   - 点击 **保存并部署**
   - 等待首次构建完成（约 2-3 分钟）
   - 构建成功后会获得一个 `*.pages.dev` 域名

### 步骤 4：测试自动部署

现在每次推送代码到 GitHub，Cloudflare Pages 都会自动构建和部署：

```bash
# 修改代码
echo "# Testing auto deploy" >> README.md

# 提交并推送
git add .
git commit -m "测试自动部署"
git push
```

推送到 GitHub 后，Cloudflare Pages 会：
1. ✅ 自动检测到新的推送
2. ✅ 触发新的构建
3. ✅ 安装依赖并构建项目
4. ✅ 部署到全球 CDN
5. ✅ 更新你的网站

你可以在 Cloudflare Dashboard 中实时查看构建日志。

### 步骤 5：配置预览部署（可选）

为 Pull Request 自动生成预览链接：

1. 进入 Cloudflare Pages 项目设置
2. 找到 **构建配置**
3. 启用 **Preview deployments**
4. 选择以下选项：
   - ✅ 在 pull request 上部署
   - ✅ 在所有分支上部署

现在每次创建 Pull Request 都会获得唯一的预览 URL。

### 步骤 6：配置自定义域名（可选）

使用自己的域名：

1. 在 Cloudflare Pages 项目中
2. 进入 **Custom domains** → **Set up a custom domain**
3. 输入你的域名（如 `docs.example.com`）
4. 配置 DNS 记录（Cloudflare 会自动添加）
5. 等待 SSL 证书自动生成

### 环境变量配置（可选）

如果需要配置环境变量（如 API 密钥）：

1. 进入项目 **Settings** → **Environment variables**
2. 添加变量：
   ```
   NODE_ENV=production
   ```
3. 选择环境（Production / Preview / Development）
4. 保存并重新部署

### 部署工作流

```
┌─────────────┐
│ 本地开发    │
│ npm run dev │
└──────┬──────┘
       │ 代码修改
       ▼
┌─────────────┐
│ Git 推送    │
│ git push    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ GitHub      │
│ 仓库更新    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Cloudflare  │
│ 触发构建    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 自动构建    │
│ npm install │
│ npm build   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 全球部署    │
│ CDN 分发    │
└─────────────┘
```

### 常见问题

**Q: 构建失败怎么办？**

A: 检查以下几点：
- 确认 Node.js 版本为 20+
- 删除 `node_modules` 和 `package-lock.json`，重新推送
- 在 Cloudflare Dashboard 查看详细构建日志

**Q: 部署后页面 404？**

A: 检查：
- 输出目录是否为 `out`
- `next.config.ts` 中是否设置了 `output: 'export'`
- `public/_redirects` 文件是否存在

**Q: 如何回滚到上一个版本？**

A: 在 Cloudflare Pages 中：
1. 进入 **部署** 标签
2. 找到之前的成功部署
3. 点击 **Rollback** 按钮

**Q: 部署时出现 "Missing entry-point to Worker script" 错误？**

A: 这个错误表示 Cloudflare Pages 项目配置错误：

1. **检查项目类型**
   - 确保创建的是 **Pages** 项目，不是 Workers 项目
   - 进入 Workers & Pages → 找到你的项目
   - 确认项目类型为 "Pages" 而不是 "Workers"

2. **重新创建项目**
   - 删除当前的 Cloudflare Pages 项目
   - 重新创建，选择 **Pages** 而不是 **Workers**
   - 确保选择 **Connect to Git** 而不是 **Direct Upload**

3. **确认构建设置**
   - 构建命令：`npm install && npm run build`
   - 构建输出目录：`out`
   - Root directory：`/` (留空)
   - Node.js 版本：`20`

4. **不要添加自定义部署命令**
   - 在项目设置中，确保没有配置自定义的部署命令
   - Cloudflare Pages 会自动处理 `out` 目录

正确的部署流程应该是：
```
构建 (npm run build) → 生成 out 目录 →
Cloudflare 自动部署 out 目录
```

如果问题持续，请参考 [Cloudflare Pages 文档](https://developers.cloudflare.com/pages/)

### 更多信息

查看 [DEPLOYMENT.md](./DEPLOYMENT.md) 获取更详细的部署指南和故障排除。

## 添加新教程

在 `data/tutorials.ts` 中添加新的教程信息：

```typescript
{
  slug: "your-tutorial-slug",
  title: "教程标题",
  description: "教程描述",
  tool: "Claude Code | Open Code | Both",
  category: "beginner | advanced | practical",
  duration: "阅读时间",
  readTime: 阅读分钟数,
  date: "发布日期"
}
```

然后在 `app/tutorials/[slug]/tutorial-content.tsx` 中添加教程内容。

## 添加新 Skill

在 `data/skills.ts` 中添加新的 Skill 信息：

```typescript
{
  id: "skill-id",
  name: "Skill 名称",
  description: "Skill 描述",
  category: "分类",
  owner: "GitHub 用户名",
  repo: "GitHub 仓库名",
  installs: 1000,
  tags: ["tag1", "tag2"],
  featured: false
}
```

支持的分类：
- 开发工具
- UI/UX
- 测试
- 文档
- 安全
- 部署
- 框架集成
- 数据处理
- AI/ML

## 添加新示例

在 `data/examples.ts` 中添加新的示例信息，并在 `app/examples/[id]/example-content.tsx` 中添加示例内容。

## 自定义主题

在 `app/globals.css` 中修改颜色变量来自定义主题：

```css
:root {
  --background: 0 0% 100%;
  --foreground: 0 0% 3.9%;
  --primary: 0 0% 9%;
  --secondary: 0 0% 96.1%;
  /* ... */
}

.dark {
  --background: 0 0% 3.9%;
  --foreground: 0 0% 98%;
  --primary: 0 0% 98%;
  --secondary: 0 0% 14.9%;
  /* ... */
}
```

## License

MIT
